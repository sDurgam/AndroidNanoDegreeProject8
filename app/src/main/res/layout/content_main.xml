<?xml version="1.0" encoding="utf-8"?>
<com.durga.sph.androidchallengetracker.ui.DPadDrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_marginTop="?android:attr/actionBarSize"
    android:descendantFocusability="afterDescendants"
    android:layoutDirection="locale"
    android:textDirection="locale"
    tools:openDrawer="start"
    tools:showIn="@layout/activity_main">

    <FrameLayout
        android:id="@+id/main_frameLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:descendantFocusability="afterDescendants"
        android:padding="@dimen/padding"
        app:layout_behavior="@string/appbar_scrolling_view_behavior"></FrameLayout>

    <android.support.design.widget.NavigationView
        android:id="@+id/main_navigationView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:contentDescription="@string/add_question_name"
        android:fitsSystemWindows="false"
        app:itemTextAppearance="?android:attr/textAppearanceSmallPopupMenu"
        app:menu="@menu/drawer_view"></android.support.design.widget.NavigationView>

</com.durga.sph.androidchallengetracker.ui.DPadDrawerLayout>
